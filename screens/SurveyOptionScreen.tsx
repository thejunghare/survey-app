import * as React from 'react';
import { SafeAreaView } from 'react-native';
import { Text } from 'react-native-paper';

const SurveyOptionScreen = () => {
    return (
        <SafeAreaView>
            <Text>Survey Option</Text>
        </SafeAreaView>
    );
}

export default SurveyOptionScreen;